# Test fixtures under tests/data are binary/data assets (.frm, .pal, .dat, ...),
# not source to analyze. Excluding them avoids false positives such as a .frm
# fixture being parsed as a VisualBasic form file.
sonar.exclusions=tests/data/**
